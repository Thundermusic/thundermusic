/* eslint-disable */

export function swDownload(getMeta) {
  return async (music, m, progressFn) => {
    const meta = await (m || getMeta(music));


  };
}

export async function cleanupMusic(music) {

}
